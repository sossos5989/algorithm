#include <iostream>
#define fastIO cin.tie(0)->sync_with_stdio(0)
using namespace std;

int main() {
    fastIO;
    int n;
    cin >> n;
    cout << n << '\n';
    while (--n) {
        cout << n << '\n';
    }
    return 0;
}