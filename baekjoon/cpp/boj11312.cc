#include <iostream>
#define fastIO cin.tie(0)->sync_with_stdio(0)
using namespace std;

int main() {
    fastIO;
    long long t, a, b;
    cin >> t;
    while (t--) {
        cin >> a >> b;
        cout << dx * dx << '\n';
    }
    return 0;
}