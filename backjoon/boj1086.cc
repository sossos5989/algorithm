#include <iostream>
#define fastIO cin.tie(0)->sync_with_stdio(0)
using namespace std;

int n, list[15], k;

// 이거,, 짐바브웨 문제랑 비슷함!

int main() {
    fastIO;
    cin >> n;
    for (int i = 0; i < n; i++) {
        cin >> list[i];
    }
    cin >> k;

    return 0;
}