#include <iostream>
#define fastIO cin.tie(0)->sync_with_stdio(0)
using namespace std;

int n, lo, hi; // n개의 약수를 가지는 lo <= k <= hi 범위 내의 수의 개수를 구하기

int main() {
    fastIO;
    int T;
    cin >> T;
    while (T--) {
        cin >> n >> lo >> hi;
    }
    return 0;
}